# ============================================================
#  OMarchy managed – DO NOT EDIT
#  This line loads the current theme colors.
#  Omarchy overwrites the theme file when you change themes.
# ============================================================
include ~/.local/state/omarchy/current/theme/kitty.conf


# ============================================================
#  YOUR SETTINGS – Edit freely below this line
# ============================================================

# Font
font_family       JetBrainsMono Nerd Font
bold_italic_font  auto
font_size         12.5

# Window
window_padding_width      1.5
hide_window_decorations   yes
confirm_os_window_close   0

# Keybindings
map ctrl+insert           copy_to_clipboard
map shift+insert          paste_from_clipboard
map shift+enter           send_text all \e[13;2u
map alt+shift+enter       send_text all \e[13;4u

# Allow remote access (needed by Omarchy)
allow_remote_control      yes
listen_on                 unix:${XDG_RUNTIME_DIR}/omarchy-kitty-{kitty_pid}

# Cursor & aesthetics
cursor_shape                      block
cursor_blink_interval             0
shell_integration                 no-cursor
enable_audio_bell                 no

# Cursor trail animation
cursor_trail                      10
cursor_trail_decay                0.05 0.3
cursor_trail_start_threshold      0

# Tab bar styling
tab_bar_edge          bottom
tab_bar_style         powerline
tab_powerline_style   slanted
tab_title_template    {title}{' :{}:'.format(num_windows) if num_windows > 1 else ''}
